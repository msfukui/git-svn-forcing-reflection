language: ruby
rvm:
  - 2.2
sudo: false
addons:
  apt:
    packages:
      - tcsh
      - git
      - subversion
      - subversion-tools
script:
  - git --version
  - svn --version
  - cd test
  - ./test_git-svn-overwrite
